Requires Instrument Approach Procedure plates
	Download these with the "downloadPlates.pl" file

Requires a database containing lat/lon info (currently included in the git repository)

Requires perl version > 5.010

Requires the following external programs
	GDAL
	mupdf-tools (sudo apt-get install mupdf-tools)
	pdftotext
	sqlite3 (sudo apt-get install sqlite3)

Requires the following CPAN modules
	PDF::API2   (sudo apt-get install libpdf-api2-perl)
	DBI
	Data::Dumper
	File::Basename
	Math::Polygon (sudo apt-get install libmath-polygon-perl)

	use Math::Trig
	use Math::Polygon
	use Acme::Tools qw(between)
	use Image::Magick
	use File::Slurp
